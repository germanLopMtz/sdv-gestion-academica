namespace sdv_backend.Domain.Enum
{
    
    public enum AttendanceStatus
    {
        None = 0,
        S = 1,   // Sí asistió
        N = 2,   // No asistió
        J = 3    // Justificado
    }
}
